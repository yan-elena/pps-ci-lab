name: pps-ci-lab
on:
  push:
jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - uses: joschi/setup-jdk@v2.3.0
        with:
          java-version: 15
      - name: Build
        run: ./gradlew check